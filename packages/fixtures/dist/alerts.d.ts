import type { Alert } from "@insightops/contracts";
/** Stable list of recent alerts (newest first) for dashboard/monitoring feeds. */
export declare function getMockAlerts(count?: number): Alert[];
/** One-off random alert used by the mock real-time stream. */
export declare function createRandomAlert(): Alert;
//# sourceMappingURL=alerts.d.ts.map
