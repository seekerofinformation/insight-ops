# API service scaffold

This workspace is intentionally empty at stage 0. It will host the REST service in the next implementation stage and consumes `@insightops/contracts` as the only shared HTTP boundary.
